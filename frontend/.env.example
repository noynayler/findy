# Google AI (client-side match analysis).
# Production Docker images: this must be available when `npm run build` runs
# (Dockerfile ARG/ENV). Set the same names as build-args / “build-time” env on Render, etc.
VITE_GEMINI_API_KEY=

# Supabase (browser + Flask job cache; same keys — backend also reads these via database.py)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
